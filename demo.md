# 短剧剧本格式示例

## 单集剧本格式（国内模式）

```
## 第{N}集

{集号}-1 {地点} {时间}/{内外}
角色：{人物列表}
△{环境与人物动作描写，交代场景}
△{人物动作描写}
**{角色名}**（{语气/方言/动作指示}）："{台词}"
**{角色名}**："{台词}"
△{关键动作或转折描写}
【{特效/动画提示}】△{特效对应的动作描写}
△{过渡或动作收束}

{集号}-2 {地点} {时间}/{内外}
角色：{人物列表}
△{场景描写}
**{角色名}**（{语气/方言}）："{台词}"
...

{集号}-3 {地点} {时间}/{内外}
角色：{人物列表}
...

```

### 格式要点

- **场次编号**：`集号-场次号`（如 `1-1`、`1-2`、`1-3`），替代"场次一/二/三"。
- **场景头一行写完**：`地点 时间/内外`（如 `长公主府门口 日/外`）。
- **人物单列一行**：`角色：A、B、C`。
- **动作描写**用 `△` 开头，不加景别括号；景别（全景/中景/特写等）由描写本身体现，需要强调时融入文字。
- **台词括注**写在角色名后：`**角色名**（语气/方言/动作）："{台词}"`；方言/语种为可选，按需标注（河南话、粤语、英语等均可）。
- **特效/动画提示**用 `【】` 单列，紧跟对应动作描写（如 `【Q版特效】`）。
- **场次之间直接连写**，无需分隔线；每集结尾按需加钩子。

---
