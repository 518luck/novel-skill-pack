# 项目说明

本仓库是短剧创作技能包（`.agents/skills/short-drama-codex/`）及配套资源。

## 资源文件自动收集

当用户在对话中提供或提到"资源文件""素材文件""收集"等内容（如 PDF、表格、文档、整理好的梗、题材资料、案例拆解等），按以下流程处理：

1. **归位**：将资源文件保存到 `resources/` 目录下。文件名用清晰的中文，能体现内容（如 `霸总题材拆解.md`、`怼人语录100条.md`），不要用 `新建文件.md` 这类名字。
2. **整理**：若是 PDF/图片等非 Markdown 格式，提取核心内容转为 `.md` 存入 `resources/`，原文件如有保留价值也一并存放。
3. **登记索引**：打开 `resources/INDEX.md`，在"索引"表格末尾追加一行，包含：
   - 文件名（带链接）
   - 类型（梗/台词、题材/案例、方法论、参考资料）
   - 一句话简介
   - 规模（条目数 / 字数 / 页数）
   - 添加日期
4. **确认**：告诉用户文件已存入 `resources/` 并登记到索引，附上路径。

## 资源查询

当用户要查找素材、找灵感、找台词时：

- 先读 `resources/INDEX.md` 定位有哪些资源
- 再按需打开对应文件检索具体内容
- 不要一次性把所有资源塞进上下文，按需加载

## 目录结构约定

```
novel-skill-pack/
├── AGENTS.md                          # 本文件（项目指令）
├── .agents/skills/short-drama-codex/  # 短剧创作技能包
│   ├── SKILL.md
│   └── references/                    # 技能内置方法论（按创作阶段加载）
├── resources/                         # 资源素材库（PDF/表格/梗等）
│   ├── INDEX.md                       # 资源索引（查询入口）
│   └── *.md                           # 各资源文件
├── docs/                              # 指南文档
│   └── 用前须知.md
└── demo.md                            # 剧本格式示例
```

**区分**：`references/` 是技能自带的创作方法论（系统知识），`resources/` 是外部收集的素材（实战资源）。两者职责不同，不要混放。
